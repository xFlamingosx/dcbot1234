first install python 3.13.0 (check by entering cmd then "python --version")
then go to the folder press on the path and type cmd to open a cmd in there
after that type in "myenv\scripts\activate"
to activate the myenv

after that just type main.py in the cmd that u opened
!!! MAKE SURE TO FILL IN THE config.py !!!


config.py tut ---

token -- discord bot token
guild_id -- discord server id
role_id -- role to give after verify id
channel_id -- channel to send the register info (verify codes and emails)
main_channel -- channel to remove the messages from and send the button to verify